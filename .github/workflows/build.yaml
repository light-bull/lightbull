name: Build
on:
  push:
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: Install go
      uses: actions/setup-go@v1
    - name: Build go
      run: make
