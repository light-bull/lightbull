name: Build
on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Install go
      uses: actions/setup-go@v3
      with:
        go-version: '>=1.18.0,<1.19.0'
    - name: Build go
      run: make
